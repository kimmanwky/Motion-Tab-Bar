name: motion_tab_bar_v2
description: A maintained replacement for Motion-Tab-Bar - an animated bottom navigation bar for Flutter with customizable colors and icons that animate into place.
version: 3.0.0
repository: https://github.com/kimmanwky/Motion-Tab-Bar
homepage: https://github.com/kimmanwky/Motion-Tab-Bar

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
